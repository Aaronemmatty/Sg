# ═══════════════════════════════════════════════════════════════════════
# AUTO-GENERATED by scripts/blue_green_deploy.sh — DO NOT EDIT BY HAND.
# Re-run `./sg deploy` (or restart from scratch) to regenerate.
#
# This file is the ONLY thing that changes when traffic flips between the
# blue and green slots: it points nginx's upstreams at whichever slot's
# containers are currently live, then blue_green_deploy.sh issues
# `nginx -s reload` to pick it up — zero nginx downtime, zero edits to
# 10-platform.conf.
#
# Initial state (first `./sg up`, before any deploy has ever run): blue.
# Active slot: blue (template default)
# ═══════════════════════════════════════════════════════════════════════

upstream dashboard    { server sg_blue_dashboard:3000; }
upstream portfolio_svc { server sg_blue_portfolio_management_service:8009; }
upstream risk_svc      { server sg_blue_risk_engine_service:8007; }
upstream execution_svc { server sg_blue_execution_engine_service:8008; }

# Singleton infra — same regardless of which slot is live.
upstream grafana { server grafana:3000; }
upstream mlflow  { server mlflow:5000; }
